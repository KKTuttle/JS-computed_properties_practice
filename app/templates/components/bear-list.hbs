<h3>Bears ({{totalBears}})</h3>
<ul>
  {{#each bears as |bear|}}
    <li {{action 'favorited' bear}}>
      {{#if bear.favorited}}
        🌟
      {{/if}}
      {{bear.name}}
    </li>
  {{/each}}
</ul>
<button {{action 'showMore'}}>Show More</button>
